[package]
name = "ai-sre"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "BodhiSpace AI SRE control-plane foundation"

[lib]
path = "src/lib.rs"

[[bin]]
name = "ai-sre"
path = "src/main.rs"

[dependencies]
axum = { version = "0.8.9", default-features = false, features = ["http1", "tokio"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
thiserror = "2.0.20"
reqwest = { version = "0.13.4", default-features = false, features = ["form", "json", "query", "rustls"] }
rig-core = { version = "=0.42.0", default-features = false, features = ["derive", "reqwest", "rustls"] }
rusqlite = { version = "0.40.2", features = ["bundled"] }
tokio = { version = "1.53.1", features = ["io-util", "macros", "net", "process", "rt", "sync", "time"] }

[dev-dependencies]
pretty_assertions = "1.4.1"

[lints.rust]
missing_docs = "deny"
